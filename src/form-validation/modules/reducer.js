let initialState = {
  listUser: [],
};

const formValidationReduder = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default formValidationReduder;
